apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: LoadBalancer
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: {{ .Values.syslogPort }}
      targetPort: {{ .Values.syslogPort }}
      protocol: TCP
      name: syslog-tcp
